# instruments.yaml — release-audit
# owner-produced-only na v1.0.0. Fase 3+ adiciona instrumentos externos se e
# quando outros owners os precisarem.
accepts:
  - producer: release-audit
    instrument: "*"
    for-checks: ["*"]
