# instruments.yaml — close-work
# Universal contract with a local adapter; accepts evidence from its own
# adapter only.
platforms: [web, desktop]
accepts:
  - producer: close-work
    instrument: adapter-local
    for-checks: ["*"]
