# {{HOST_AGENT_FILE}} — {{PROJECT_NAME}}

Map of what anyone (human or agent) working here needs to know.

## Architecture

{{ARCH_DESCRIPTION}}

## Commands

- Build: `{{BUILD_COMMAND}}`
- Test: `{{TEST_COMMAND}}`
- Typecheck: `{{TYPECHECK_COMMAND}}`
- Lint: `{{LINT_COMMAND}}`
- Run locally: `{{RUN_COMMAND}}`

Measured numbers (test counts, bundle size) live in `ESTADO.md` with the
command and HEAD that produced them — never here.

## This project's invariants (do not reopen)

{{INVARIANTS_LIST}}

## Conventions

{{CONVENTIONS}}

## Installed skills

- Plugin `production-quality-ready`
  — generic owners; `platform: {{PLATFORM}}` in `{{HOST}}/gates.json`.
- Project adapters in `{{HOST}}/skills/`:
  - `start-work` — open a session: state, baseline, ownership table.
  - `review-change` — pre-close gate: invariants + adversarial axes {{AXES_THAT_APPLY}}.
  - `close-work` — rewrite `ESTADO.md`, one subject one owner.
  - `verify` — run and prove in the real app.

## Contribution rules

- Run `{{TYPECHECK_COMMAND}}`, `{{LINT_COMMAND}}`, `{{TEST_COMMAND}}` and
  the `review-change` matrix before calling any change done.
{{CONTRIBUTION_RULES}}

---

Generated by `bootstrap-project` on {{DATE}}. Edit
freely afterwards; the file belongs to the project.
