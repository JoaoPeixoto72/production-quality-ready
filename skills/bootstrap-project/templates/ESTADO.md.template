# ESTADO.md — {{PROJECT_NAME}}

Where the project is now and what comes next. Rewritten at the close of
every work session by `{{HOST}}/skills/close-work`.

## 1. Where we are

- Version: {{VERSION}}
- Build: {{BUILD_STATUS}} (`{{BUILD_COMMAND}}` @ HEAD {{HEAD}}, {{DATE}})
- Tests: {{TEST_STATUS}} (`{{TEST_COMMAND}}` @ HEAD {{HEAD}}, {{DATE}})
- Typecheck: {{TYPECHECK_STATUS}} (`{{TYPECHECK_COMMAND}}`)
- Last audit: none yet — run `run-all-owners.ps1` then `audit-app`.

## 2. Next

(the concrete next step, first move already written — not "improve X"
but "open file Y and add Z")

## 3. Waiting on another machine, or on money

## 4. To eyeball manually

## 5. Ideas, for gain

---

Generated by `bootstrap-project` v{{PLUGIN_VERSION}} on {{DATE}}.
