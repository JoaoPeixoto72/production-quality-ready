# ESTADO.md

Where the project is now, and what comes next. Rewrites itself at the
close of every work session — see `.claude/skills/close-work`.

## 1. Where we are

- Version: {{VERSION}}
- Compiles: {{BUILD_STATUS}} (`{{BUILD_COMMAND}}`, {{DATE}})
- Tests: {{TEST_STATUS}} ({{TEST_COUNT}} tests, `{{TEST_COMMAND}}`, {{DATE}})
- Last audit: (none yet — run `audit-app`)

## 2. Next

(fill with the concrete next step, with the first move already written —
not "improve X", but "open file Y and add field Z")

## 3. Waiting on another machine, or on money

(what can't be done from here)

## 4. To eyeball manually

(what no test catches)

## 5. Ideas, for gain

(none promised to anyone)

---

Generated by `bootstrap-project` v1.0.0 on {{DATE}}.
