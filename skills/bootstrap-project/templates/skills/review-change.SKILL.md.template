---
name: review-change
description: Reviews a change in {{PROJECT_NAME}} before it is called done. Use after writing or changing code and before close-work. Applies this project's invariants, the plugin's adversarial axes for platform {{PLATFORM}}, and runs the proof commands. Not for auditing the whole app (audit-app) or opening a session (start-work).
contract: {{CONTRACT_PATH}}
extends: "production-quality-ready::review-change@2.x"
version: 2.0.0
allowed-tools: Read, Glob, Grep, Bash, Edit, Write
---

# review-change in {{PROJECT_NAME}}

Local adapter. The adversarial matrix (A1–A11) lives in
`{{HOST}}/plugins/production-quality-ready/skills/review-change/SKILL.md`;
this file only instantiates it.

## Platform

`platform: {{PLATFORM}}` (from `{{HOST}}/gates.json`). Axes that apply:
{{AXES_THAT_APPLY}}.

## This project's invariants

{{PROJECT_INVARIANTS}}

## Where to look

{{AXES_LOOKUP_TABLE}}

## Proof commands

```bash
{{TYPECHECK_COMMAND}}
{{LINT_COMMAND}}
{{BUILD_COMMAND}}
{{TEST_COMMAND}}
{{MIGRATIONS_VERIFY_COMMAND}}
```

Counts live in `ESTADO.md` — never here.
