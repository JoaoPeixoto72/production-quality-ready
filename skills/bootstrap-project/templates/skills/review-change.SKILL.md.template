---
name: review-change
description: Review a change in {{PROJECT_NAME}} before calling it done. Local adapter for production-quality-ready's review-change — inherits the Universal Adversarial Matrix (concurrency, input tampering, IDOR, webhooks, XSS, WCAG AA, DB immutability) and applies {{PROJECT_NAME}}'s specific invariants. Use after writing or changing code, before `close-work`. Do NOT use to audit the whole app (audit-app, plugin). Do NOT use to open a work session — that's `start-work`.
contract: {{CONTRACT_PATH}}
extends: "production-quality-ready::review-change@1.x"
version: 1.1.0
allowed-tools: Read, Glob, Grep, Bash, Edit, Write
---

# review-change in {{PROJECT_NAME}}

Local adapter. Universal contract and the 7-axis Adversarial Matrix live in
`production-quality-ready/skills/review-change/SKILL.md`; adapter contract in
`production-quality-ready/adapter-contracts/review-change.md`.

## Regra Fundacional

Testes a passar são o requisito mínimo, nunca a prova de aprovação.
Toda a alteração deve ser avaliada contra os **Invariantes do {{PROJECT_NAME}}** abaixo e a **Matriz Adversarial Universal** herdada do plugin.

## This project's invariants

{{PROJECT_INVARIANTS}}

## Proof commands

```bash
{{BUILD_COMMAND}}
{{TEST_COMMAND}}
```

Counts live in `docs/VERIFICAR.md` — never here.
