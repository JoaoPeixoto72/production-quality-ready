---
name: {{ADAPTER_NAME}}
description: "Review a change to {{PROJECT_NAME}} after writing code, before close-work: this project's invariants, the adversarial axes for its platform, the proof commands. Adapter of review-change; not for whole-app audits."
contract: production-quality-ready/CONTRACTS.md
extends: "production-quality-ready:review-change"
allowed-tools: Read, Glob, Grep, Bash, Edit, Write
---

# review-change in {{PROJECT_NAME}}

Local adapter. The adversarial matrix (A1–A11) and the rationalisations
table live in the plugin skill `production-quality-ready:review-change`;
this file only instantiates them.

## Platform

`platform: {{PLATFORM}}` (from `{{HOST}}/gates.json`). Axes that apply:
{{AXES_THAT_APPLY}}.

## This project's invariants

{{PROJECT_INVARIANTS}}

## Where to look

{{AXES_LOOKUP_TABLE}}

## Proof commands

```bash
{{TYPECHECK_COMMAND}}
{{LINT_COMMAND}}
{{BUILD_COMMAND}}
{{TEST_COMMAND}}
{{MIGRATIONS_VERIFY_COMMAND}}
```

Counts live in `ESTADO.md` — never here.
