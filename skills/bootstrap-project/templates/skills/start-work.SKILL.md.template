---
name: {{ADAPTER_NAME}}
description: "Open work on {{PROJECT_NAME}} at the start of every conversation, before touching files: read ESTADO.md, run the baseline, find the owner of the code to change. Adapter of start-work; not for review or close."
contract: production-quality-ready/CONTRACTS.md
extends: "production-quality-ready:start-work"
allowed-tools: Read, Glob, Grep, Bash, Edit, Write
---

# start-work in {{PROJECT_NAME}}

Local adapter of the plugin skill `production-quality-ready:start-work`,
which holds the rule; this file holds only what is {{PROJECT_NAME}}'s.

## Where the state is

`ESTADO.md` at the repo root. Read §1 and §2 before anything else.

## Baseline

```bash
{{BUILD_COMMAND}}
{{TEST_COMMAND}}
```

Every reported number carries its command and HEAD hash.

## Where code tends to repeat in this project

{{PROJECT_SPECIFIC_OWNERSHIP_TABLE}}

## Documents that are not to be reopened

{{DOCS_NOT_TO_REOPEN}}
