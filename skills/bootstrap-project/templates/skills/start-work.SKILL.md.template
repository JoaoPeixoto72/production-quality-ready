---
name: start-work
description: Opens a work session on {{PROJECT_NAME}}. Use at the start of every conversation before touching any file, and again mid-session before new code. Reads ESTADO.md, runs the baseline, names the canonical owner of the code about to change. Not for closing (close-work), reviewing (review-change) or auditing (audit-app).
contract: {{CONTRACT_PATH}}
extends: "production-quality-ready::start-work@2.x"
version: 2.0.0
allowed-tools: Read, Glob, Grep, Bash, Edit, Write
---

# start-work in {{PROJECT_NAME}}

Local adapter. Universal contract:
`{{HOST}}/plugins/production-quality-ready/skills/start-work/SKILL.md`.

## Where the state is

`ESTADO.md` at the repo root. Read §1 and §2 before anything else.

## Baseline

```bash
{{BUILD_COMMAND}}
{{TEST_COMMAND}}
```

Every reported number carries its command and HEAD hash.

## Where code tends to repeat in this project

{{PROJECT_SPECIFIC_OWNERSHIP_TABLE}}

## Documents that are not to be reopened

{{DOCS_NOT_TO_REOPEN}}
