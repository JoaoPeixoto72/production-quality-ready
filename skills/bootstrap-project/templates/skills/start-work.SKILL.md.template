---
name: start-work
description: Open a work session on {{PROJECT_NAME}} — local adapter. Cites start-work from the production-quality-ready plugin as its base. Use at the start of every conversation, before touching any file, and again mid-session before writing new code. Do NOT use to close (`close-work`), review (`review-change`) or audit (audit-app, plugin).
contract: {{CONTRACT_PATH}}
extends: "production-quality-ready::start-work@1.x"
version: 1.0.0
allowed-tools: Read, Glob, Grep, Bash, Edit, Write
---

# start-work in {{PROJECT_NAME}}

Local adapter. Universal contract lives in
`production-quality-ready/skills/start-work/SKILL.md`; adapter contract in
`production-quality-ready/adapter-contracts/start-work.md`.

## Where the state is

`ESTADO.md` at the repo root.

## Proof command

```bash
{{BUILD_COMMAND}}
{{TEST_COMMAND}}
```

Every reported number carries the command and HEAD hash alongside.

## Where code tends to repeat in this project

{{PROJECT_SPECIFIC_OWNERSHIP_TABLE}}

## Documents that are not to be reopened

- `docs/decisoes/` — decisions with declared reason.
- `docs/factos-verificados/` — numbers with the command that produced
  them.
- `docs/auditorias/` — audit reports.
