---
name: {{ADAPTER_NAME}}
description: "Close work on {{PROJECT_NAME}} after review, before commit: rewrite ESTADO.md, one subject per document, reason stays and history goes, numbers with command and HEAD. Adapter of close-work; not for opening."
contract: {{CONTRACT_PATH}}
extends: "production-quality-ready::close-work@2.x"
version: 2.0.0
allowed-tools: Read, Glob, Grep, Bash, Edit, Write
---

# close-work in {{PROJECT_NAME}}

Local adapter of the plugin skill `production-quality-ready:close-work`,
which holds the rule; this file holds only what is {{PROJECT_NAME}}'s.

## Document → subject table (one subject, one owner)

{{DOC_OWNERSHIP_TABLE}}

## Pre-commit proof

```bash
{{BUILD_COMMAND}}
{{TEST_COMMAND}}
```

Write the numbers into `ESTADO.md` §1 with command + HEAD + date.

## When to bump version

{{VERSION_BUMP_RULES}}
