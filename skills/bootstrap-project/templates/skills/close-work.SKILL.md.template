---
name: close-work
description: Update {{PROJECT_NAME}}'s documents at the end of a work session. Local adapter for production-quality-ready's close-work — declares this project's document→subject table and rewrites the ESTADO.md. Rule "one subject, one owner; reason stays, history goes". Use at the end of every work session, before commit. Do NOT use to open — that's `start-work`. Do NOT use to review code first — that's `review-change` (comes first).
contract: {{CONTRACT_PATH}}
extends: "production-quality-ready::close-work@1.x"
version: 1.0.0
allowed-tools: Read, Glob, Grep, Bash, Edit, Write
---

# close-work in {{PROJECT_NAME}}

Local adapter. Universal contract lives in
`production-quality-ready/skills/close-work/SKILL.md`; adapter contract in
`production-quality-ready/adapter-contracts/close-work.md`.

## Document → subject table (rule "one subject, one owner")

{{DOC_OWNERSHIP_TABLE}}

## Pre-commit proof command

```bash
{{BUILD_COMMAND}}
{{TEST_COMMAND}}
```

Counts live in `docs/VERIFICAR.md` — never here.

## When to bump version

{{VERSION_BUMP_RULES}}
