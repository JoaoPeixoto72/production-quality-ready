---
name: close-work
description: Closes a work session on {{PROJECT_NAME}}. Use at the end of every session, after review-change and before commit. Rewrites ESTADO.md, applies "one subject, one owner; reason stays, history goes", records the proof numbers with command and HEAD. Not for opening (start-work) or reviewing code (review-change).
contract: {{CONTRACT_PATH}}
extends: "production-quality-ready::close-work@2.x"
version: 2.0.0
allowed-tools: Read, Glob, Grep, Bash, Edit, Write
---

# close-work in {{PROJECT_NAME}}

Local adapter. Universal contract:
`{{HOST}}/plugins/production-quality-ready/skills/close-work/SKILL.md`.

## Document → subject table (one subject, one owner)

{{DOC_OWNERSHIP_TABLE}}

## Pre-commit proof

```bash
{{BUILD_COMMAND}}
{{TEST_COMMAND}}
```

Write the numbers into `ESTADO.md` §1 with command + HEAD + date.

## When to bump version

{{VERSION_BUMP_RULES}}
