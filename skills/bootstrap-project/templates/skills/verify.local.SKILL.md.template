---
name: verify
description: Run {{PROJECT_NAME}} and drive it through the window to prove changes. Local adapter for the universal verify — concrete launch commands, artifact paths, project-specific traps. The harness that drives the Win32/WebView2 window is drive-app-window, in the plugin. Use for "see this change working". Do NOT use to run tests (that's code, cd + cargo test) or to audit the app (audit-app, plugin).
contract: {{CONTRACT_PATH}}
extends: "production-quality-ready::verify@1.x"
requires-adapter: false
version: 1.0.0
allowed-tools: Read, Glob, Grep, Bash
---

# Run {{PROJECT_NAME}}

Local adapter of the universal `verify`. Everything the harness needs
that is **specific to this project**.

## Launch

```bash
{{RUN_COMMAND}}
```

Binaries and models live in `{{DATA_DIR}}`.

## Artifacts

{{PROJECT_ARTIFACTS_TABLE}}

## Traps

None known specific to this project yet. Add them here as they surface.

## Pilot projects

Add small, fast-loading projects here as they are identified.
