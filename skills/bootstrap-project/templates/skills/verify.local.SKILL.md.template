---
name: {{ADAPTER_NAME}}
description: "Run {{PROJECT_NAME}} and prove a change in the real app: launch command, surfaces, test credentials, where it writes. Use to see a flow, screen or endpoint working. Adapter of verify; not for the test suite."
contract: {{CONTRACT_PATH}}
extends: "production-quality-ready::verify@2.x"
requires-adapter: false
version: 2.0.0
allowed-tools: Read, Glob, Grep, Bash
---

# Run and verify {{PROJECT_NAME}}

Local adapter of the universal `verify`. Platform `{{PLATFORM}}`; driver
`{{VERIFY_DRIVER}}`.

## Launch

```bash
{{RUN_COMMAND}}
```

Base URL / entry point: `{{BASE_URL}}`

## Surfaces worth proving

{{PROJECT_SURFACES_TABLE}}

## Test credentials

{{TEST_CREDENTIALS}}

## Artefacts the app writes

{{PROJECT_ARTIFACTS_TABLE}}

## Traps

{{PROJECT_TRAPS}}
