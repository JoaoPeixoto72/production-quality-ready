---
name: verify
description: Runs {{PROJECT_NAME}} locally and proves changes in the real app. Use for "see this working" or manual verification of a flow, screen or endpoint. Gives the launch command, surfaces, test credentials and artefact paths; driver is {{VERIFY_DRIVER}}. Not for running the test suite (code-review) or auditing (audit-app).
contract: {{CONTRACT_PATH}}
extends: "production-quality-ready::verify@2.x"
requires-adapter: false
version: 2.0.0
allowed-tools: Read, Glob, Grep, Bash
---

# Run and verify {{PROJECT_NAME}}

Local adapter of the universal `verify`. Platform `{{PLATFORM}}`; driver
`{{VERIFY_DRIVER}}`.

## Launch

```bash
{{RUN_COMMAND}}
```

Base URL / entry point: `{{BASE_URL}}`

## Surfaces worth proving

{{PROJECT_SURFACES_TABLE}}

## Test credentials

{{TEST_CREDENTIALS}}

## Artefacts the app writes

{{PROJECT_ARTIFACTS_TABLE}}

## Traps

{{PROJECT_TRAPS}}
