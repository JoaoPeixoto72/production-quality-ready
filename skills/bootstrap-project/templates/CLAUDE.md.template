# CLAUDE.md — {{PROJECT_NAME}}

Map of what anyone working here needs to know.

## Architecture

{{ARCH_DESCRIPTION}}

## Commands

- Build: `{{BUILD_COMMAND}}` (measured {{DATE}}: {{BUILD_RESULT}})
- Test: `{{TEST_COMMAND}}` (measured {{DATE}}: {{TEST_COUNT}} tests)
- Typecheck: `{{TYPECHECK_COMMAND}}`

## This project's invariants (do not reopen)

{{INVARIANTS_LIST}}

## Conventions

{{CONVENTIONS}}

## Installed skills

- Plugin `production-quality-ready` (owners + orchestrator + capabilities)
- Local skills in `.claude/skills/`:
  - `start-work` — local adapter for `start-work`
  - `review-change` — local adapter for `review-change`
  - `close-work` — local adapter for `close-work`
  - `verify` — local adapter for universal `verify`

## Contribution rules

{{CONTRIBUTION_RULES}}

---

Generated by `bootstrap-project` v1.0.0 on {{DATE}}. Edit freely
afterwards; the file belongs to the project, not the generator.
