# instruments.yaml — review-change
# Contrato universal com adapter local. Aceita evidência do próprio owner
# (o próprio adapter local).
platforms: [web, desktop]
accepts:
  - producer: review-change
    instrument: adapter-local
    for-checks: ["*"]
