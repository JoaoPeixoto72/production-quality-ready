# Video Editor — Upstream Notes

Recommended font pair for this profile:

- `@fontsource-variable/instrument-sans@5.3.0`
- `@fontsource-variable/jetbrains-mono@5.3.0`

These packages and licenses are references only. This skill package does not
ship the font binaries or notices; the consumer project must vendor and load
them locally.

For Tauri, validate early that local `woff2` files render correctly inside the
webview on every supported platform.
